#include "global.h"
#include "debug.h"

/**
 * Compares the two strings given. Returns whether or not they are equal.
 * @param str1 a string of any length
 * @param str2 a string of any length
 * @returns 0 if the strings are different, else 1
*/
int compareStrings(char* str1, char* str2);